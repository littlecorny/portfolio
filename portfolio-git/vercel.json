{
  "version": 2,
  "builds": [
    {
      "src": "portfolio-git/package.json",
      "use": "@vercel/static-build",
      "config": { "distDir": "portfolio-git/dist" }
    }
  ],
  "routes": [
    { "src": "/(.*)", "dest": "/index.html" }
  ]
}
